# Toss billing keys belong in backend/.env. The frontend receives only the public
# client key from GET /api/billing/config.
VITE_API_BASE_URL=
# Public web address used in family invitation links, including links shared from the native app.
VITE_PUBLIC_APP_URL=
