# Browser-safe public keys only. Restart Vite after changing these values.
VITE_KAKAO_JAVASCRIPT_KEY=

# Toss billing keys belong in backend/.env. The frontend receives only the public
# client key from GET /api/billing/config.
