# AI 가족 운영 에이전트 — 퍼포먼스 트래커

> 작성 기준일: 2026-09-23  
> 기준 코드: 현재 `main` 브랜치의 가족방·일정·역할 배정·인수인계·알림·AI 채팅·가전 알림·결제 기능  
> 측정 단위: 기본 30일, 필요 시 1~365일로 변경

---

## 1. 지표 설계 목적

서비스의 고객 경험 목표를 다음 세 가지 핵심 가치로 정의하고, 실제 사용 기록으로 확인할 수 있는 BX·CX·DX 지표로 전환한다.

| 핵심 가치 | 고객 경험 목표 | 대표 측정 지표 |
|---|---|---|
| 조율 부담 감소 | 가족 누구도 돌봄 조율의 책임을 혼자 짊어지지 않는다 | 충돌 감지 후 재배정 완료 시간, AI 담당자 추천 채택률, 배정 요청 응답률 |
| 인계 부담 감소 | 담당자가 바뀌어도 아이의 돌봄 정보가 끊기지 않는다 | 돌봄 완료율, 인수인계 확인률 |
| 확인 부담 감소 | 사용자가 계속 앱을 확인하지 않아도 중요한 일을 놓치지 않는다 | 알림 확인률, AI 채널 활용 비율, 가전 알림 활성화율 |

퍼포먼스 트래커는 다음 심사 기준을 충족하도록 구성한다.

1. **지표 설계:** 고객 경험 목표마다 정의·산식·목표값을 둔다.
2. **측정 구현:** 서비스 데이터베이스와 이용 이벤트에서 지표를 실제 수집하고 API로 확인한다.

---

## 2. 전체 KPI 목록

### 2.1 BX Performance Tracker — 비즈니스 경험

| ID | 핵심 KPI | 정의 및 산식 | 베타 목표 | 실제 측정 수단 |
|---|---|---|---|---|
| BX-01 | 가족방 생성량 | 측정 기간에 새로 생성된 가족방 수 | 주간·월간 추이 상승 | `family_group.created_at` |
| BX-02 | 초대 링크당 참여 인원 | 초대 링크 누적 참여 인원 ÷ 생성된 초대 링크 수 | 링크당 1명 이상 | `family_invite_link.join_count` |
| BX-03 | 현재 Pro 전환율 | Pro 가족방 수 ÷ 전체 가족방 수 × 100 | 10% 이상 | `family_group.plan` |
| BX-04 | Paywall→Pro 시작 클릭률 | Pro 시작 버튼을 누른 가족방 수 ÷ Pro 안내를 본 가족방 수 × 100 | 25% 이상 | `pro_paywall_viewed`, `pro_cta_clicked` 이벤트 |
| BX-05 | Pro 시작→결제 전환율 | 결제를 완료한 가족방 수 ÷ Pro 시작 버튼을 누른 가족방 수 × 100 | 15% 이상 | 이용 이벤트 + `payment_transaction` |
| BX-06 | 한도 도달 후 24시간 내 결제 전환율 | 기능 한도 도달 후 24시간 이내 결제한 건수 ÷ 전체 한도 도달 건수 × 100 | 기능별 비교 후 상위 전환 요인 확인 | `feature_limit_reached` 이벤트 + 결제 승인 시각 |
| BX-07 | 기간 내 결제 매출 | 측정 기간에 승인 완료된 결제 금액 합계 | 월별 추이 상승 | `payment_transaction.status = DONE` |
| BX-08 | 활성 구독 유지율 | 활성 상태 구독 수 ÷ 전체 구독 수 × 100 | 90% 이상 | `family_subscription.status` |
| BX-S01 | 가족 구성원 수별 Pro 전환율 | 보호자 3명 이하/4명 이상 가족방의 Pro 전환율 비교 | 다인 가족 전환 효과 확인 | 가족 구성원 수와 `family_group.plan` 결합 집계 |

BX-01은 가족방 생성량을 직접 집계한다. BX-04~BX-06은 단순 결제 건수만 보는 대신 사용자가 어떤 기능과 화면을 거쳐 결제했는지 확인한다.

### 2.2 CX Performance Tracker — 고객 경험

| ID | 핵심 KPI | 정의 및 산식 | 베타 목표 | 실제 측정 수단 |
|---|---|---|---|---|
| CX-01 | 월간 활성 가족방 | 기간 내 앱을 1회 이상 이용한 고유 가족방 수 | 전월 대비 상승 | `app_opened` 이벤트 + 가족 세션 최종 접속 시각 |
| CX-02 | 월간 활성 구성원 | 기간 내 앱을 1회 이상 이용한 고유 구성원 수 | 가족방당 2명 이상 | `app_opened` 이벤트 + 가족 세션 최종 접속 시각 |
| CX-03 | D+7 리텐션 | 최초 이용 후 7~13일 사이 재이용 구성원 수 ÷ 측정 가능 구성원 수 × 100 | 40% 이상 | `app_opened` 이벤트 |
| CX-04 | D+30 리텐션 | 최초 이용 후 30~36일 사이 재이용 구성원 수 ÷ 측정 가능 구성원 수 × 100 | 25% 이상 | `app_opened` 이벤트 |
| CX-05 | 첫 가치 도달 평균 시간 | 가족방 생성부터 첫 일정·알림장·배정 기록까지 걸린 시간의 평균 | 10분 이내 | 가족방 생성 시각과 최초 핵심 기능 기록 시각 비교 |
| CX-06 | 알림 확인률 | 읽은 알림 수 ÷ 발송된 알림 수 × 100 | 70% 이상 | `notification.is_read`, `notification_opened` 이벤트 |
| CX-07 | AI 채널 활용 비율 | 알림 확인 및 AI 질문 건수 ÷ 주요 정보 확인 건수 × 100 | 70% 이상 | 알림 확인·챗봇 질문·주요 화면 조회 이벤트 |
| CX-08 | 인수인계 확인률 | 다음 담당자가 확인한 인수인계 수 ÷ 생성된 인수인계 수 × 100 | 95% 이상 | `care_handoff.status` |

CX 지표는 만족도를 추정해 숫자로 만들지 않고, 실제 재이용·확인·업무 완료 행동을 기준으로 계산한다. 사용자가 아직 리텐션 측정 기간에 도달하지 않았거나 분모가 0인 경우 결과는 `0%`로 왜곡하지 않고 `null`로 반환한다.

### 2.3 DX Performance Tracker — 디지털 경험

| ID | 핵심 KPI | 정의 및 산식 | 베타 목표 | 실제 측정 수단 |
|---|---|---|---|---|
| DX-01 | 일정 등록량 | 개인 일정 및 아이 일정 등록 건수 합계 | 가족방당 주 3건 이상 | 개인 일정 등록 이벤트 + `child_schedule.created_at` |
| DX-02 | OCR 알림장 등록량 | 사진 OCR로 등록한 알림장 건수 | 주간 이용 가족 증가 | `care_intake.input_type = PHOTO_TRANSCRIPT` |
| DX-03 | 충돌→재배정 완료 평균 시간 | `reassignment_confirmed` 시각 − `conflict_detected` 시각의 평균 | 평균 4분 이내 | 같은 돌봄 항목 ID로 연결된 이벤트 시각 |
| DX-04 | AI 담당자 추천 채택률 | AI 역할 매칭으로 요청해 확정·완료된 배정 수 ÷ 전체 AI 역할 매칭 요청 수 × 100 | 60% 이상 | `care_assignment.source = ROLE_MATCH` |
| DX-05 | 배정 요청 응답률 | 수락·거절 응답이 기록된 요청 수 ÷ 전체 응답 대상 요청 수 × 100 | 80% 이상 | `care_assignment.responded_at` |
| DX-06 | 돌봄 완료율 | 완료된 배정 수 ÷ 수락 또는 완료 상태 배정 수 × 100 | 90% 이상 | `care_assignment.status` |
| DX-07 | 구성원당 AI 질문 수 | 사용자가 보낸 AI 질문 수 ÷ 활성 구성원 수 | 월 3회 이상 | `assistant_message.role = user` |
| DX-08 | Pro 가족 가전 알림 활성화율 | 가전 알림을 켠 Pro 가족방 수 ÷ 전체 Pro 가족방 수 × 100 | 30% 이상 | `notification_preference.device_enabled` |

DX-03은 조율 부담 감소의 대표 지표다. 일정 충돌이 실제로 감지된 시점과 새 담당자가 확정된 시점을 같은 돌봄 항목 ID로 연결해 계산한다.

---

## 3. 자동 수집 이벤트

| 영역 | 이벤트명 | 기록 시점 | 수집 속성 예시 |
|---|---|---|---|
| CX | `app_opened` | 서비스 최초 진입 및 앱이 다시 활성화된 때 | 진입 화면, 재활성화 여부 |
| CX | `screen_view` | 화면이 바뀐 때 | 화면 ID |
| DX | `schedule_created` | 개인 일정이 저장된 때 | 일정 유형, 반복 여부 |
| DX | `conflict_detected` | 개인 일정과 돌봄 배정의 충돌을 찾은 때 | 돌봄 항목 ID, 배정 ID |
| DX | `ai_candidate_suggested` | AI가 가능한 담당자 후보를 제시한 때 | 전체·가능 후보 수 |
| DX | `request_sent` | 돌봄 담당 요청을 보낸 때 | 배정 ID, 배정 방식 |
| DX | `request_responded` | 담당 후보가 수락 또는 거절한 때 | 응답 상태, 배정 방식 |
| DX | `reassignment_confirmed` | 새 담당자가 최종 확정된 때 | 배정 ID, 확정 방식 |
| DX | `manual_adjustment_used` | 사용자가 직접 대체 담당자를 고른 때 | 대체 담당자 선택 여부 |
| DX | `assignment_completed` | 담당자가 돌봄 완료를 누른 때 | 메모·사진 첨부 여부 |
| DX | `handoff_completed` | 다음 담당자에게 인수인계를 만든 때 | 배정 ID, 메모·사진 여부 |
| DX | `handoff_acknowledged` | 다음 담당자가 인수인계를 확인한 때 | 인수인계 ID |
| DX | `notification_sent` | 앱 알림을 저장·발송한 때 | 채널, 중요도, 이동 기능 |
| DX | `notification_opened` | 사용자가 알림을 처음 확인한 때 | 알림 ID, 이동 기능 |
| DX | `chatbot_query` | AI 채팅 질문을 보낸 때 | 텍스트·음성 입력 구분, 플랜 |
| DX | `chatbot_response_delivered` | AI 답변이 완료된 때 | 토큰 수, 카드 수, 일정 처리 수 |
| DX | `chatbot_action_opened` | AI 답변 카드에서 관련 화면을 연 때 | 이동 화면 ID |
| DX | `device_alert_used` | 가전 알림을 대기열에 넣거나 테스트한 때 | 채널, 기기 ID, 전달 가능 여부 |
| BX | `family_created` | 새 가족방이 생성된 때 | 최초 플랜 |
| BX | `invite_created` | 초대 링크가 만들어진 때 | 유효 기간 |
| BX | `invite_accepted` | 초대받은 구성원이 가족방에 들어온 때 | 역할, 재사용 링크 여부 |
| BX | `feature_limit_reached` | Free 인원·아이 제한 등에 도달한 때 | 기능, 현재 수, 제한 수 |
| BX | `limit_reached_screen_viewed` | 유료 기능 안내창을 본 때 | 유도 기능 |
| BX | `pro_paywall_viewed` | Pro 안내 화면 또는 유료 기능 안내창을 본 때 | 진입 위치, 유도 기능 |
| BX | `pro_cta_clicked` | Pro 시작하기를 누른 때 | 월간·연간, 진입 위치 |
| BX | `payment_completed` | 결제가 승인되어 Pro가 활성화된 때 | 금액, 결제 방식, 주기 |
| BX | `payment_abandoned` | 결제창에서 취소 또는 실패한 때 | 이탈 단계 |
| BX | `pro_feature_used` | 긴급 도움 요청 등 Pro 기능을 사용한 때 | 기능 ID |
| BX | `subscription_renewed` | 자동 갱신 결제가 완료된 때 | 금액, 결제 방식, 주기 |
| BX | `subscription_cancelled` | 구독 취소를 예약한 때 | 기간 종료 시 취소 여부 |

일정 제목, AI 질문 원문, 인수인계 메모, 주소, 사진은 퍼포먼스 이벤트에 저장하지 않는다. 이벤트에는 집계에 필요한 내부 ID와 화면·상태·건수만 저장한다.

---

## 4. 측정 구현

### 4.1 저장 구조

`performance_event` 테이블에 다음 항목을 저장한다.

| 항목 | 용도 |
|---|---|
| `event_name` | 어떤 행동 또는 시스템 처리가 발생했는지 구분 |
| `tracker` | BX·CX·DX 영역 구분 |
| `family_id`, `member_id` | 가족방·구성원 단위 집계 |
| `correlation_id` | 충돌 감지와 재배정처럼 전후 이벤트 연결 |
| `properties` | 화면, 상태, 건수 등 비식별 분석 속성 |
| `occurred_at` | 기간·리텐션·소요시간 계산 |

가족방이 삭제되면 해당 가족방의 퍼포먼스 이벤트도 함께 삭제된다.

### 4.2 수집 API

| API | 용도 |
|---|---|
| `POST /api/performance/events` | 프론트엔드 화면 방문·앱 진입·결제 퍼널 이벤트 기록 |
| `GET /api/performance/event-counts?days=30` | 기간 내 이벤트별 발생 건수 확인 |
| `GET /api/performance/summary?days=30` | 현재 가족방의 BX·CX·DX KPI 집계 |
| `GET /api/performance/summary?days=30&scope=service` | 개발자 모드에서 전체 서비스 KPI 집계 |

전체 서비스 지표는 개인 내용 없이 건수·비율·평균만 반환한다. `scope=service`는 개발자 모드에서만 사용할 수 있다.

### 4.3 확인 방법

1. 백엔드 서버를 실행한다.
2. `http://127.0.0.1:8000/docs`에서 `performance tracker` API 그룹을 연다.
3. 가족방 단위는 `/api/performance/summary?days=30`으로 확인한다.
4. 개발용 전체 집계는 백엔드 환경 변수 `LGDX_DEV_MODE=1`에서 `/api/performance/summary?days=30&scope=service`로 확인한다.
5. 집계 기간은 `days` 값을 1~365 사이로 바꿔 비교한다.

응답에는 각 KPI의 ID, 이름, 값, 단위, 분자, 분모, 목표, 원천 데이터가 함께 포함된다. 따라서 결과 숫자뿐 아니라 산식과 데이터 출처도 같은 화면에서 검증할 수 있다.

---

## 5. 지표 해석 기준

| 상황 | 해석 원칙 |
|---|---|
| 분모가 0인 비율 | 임의로 0% 처리하지 않고 `null`로 표시 |
| 리텐션 대상 기간 미도달 | 해당 사용자를 D+7·D+30 분모에서 제외 |
| 반복 일정 일괄 등록 | 실제 생성된 일정 인스턴스 수를 등록량으로 집계 |
| 동일 알림 재조회 | 최초 읽음 처리 시에만 `notification_opened` 기록 |
| 결제 재호출 | 이미 처리된 주문은 중복 결제·중복 전환 이벤트에서 제외 |
| 개발자용 Pro 미리보기 | 실제 승인 결제가 아니므로 결제 매출과 결제 전환에서 제외 |

베타 목표값은 초기 가설이다. 최소 4주간 실제 데이터를 수집한 뒤 표본 수, 가족 구성, Free·Pro 이용 비중을 함께 보고 목표값을 조정한다.

---

## 6. 심사 기준 대응표

| 심사 기준 | 반영 내용 | 확인 근거 |
|---|---|---|
| 고객 경험 목표가 측정 가능한 지표로 정의되었는가 | 조율·인계·확인 부담을 각각 시간·채택률·응답률·완료율·확인률로 정의 | 1장 및 BX·CX·DX KPI 표 |
| KPI의 정의와 계산식이 명확한가 | 모든 KPI에 분자·분모·단위·베타 목표를 명시 | 2장 및 집계 API 응답 |
| 지표를 실제로 수집하는 수단이 구현되었는가 | 프론트 화면 이용 이벤트, 백엔드 업무 이벤트, 운영 테이블 집계를 함께 구현 | `performance_event`, 자동 기록 지점, 수집 API |
| 수집한 지표를 확인할 수 있는가 | 기간·범위별 KPI 요약 및 이벤트 건수 API 제공 | `/api/performance/summary`, `/api/performance/event-counts` |
| 개인정보를 과도하게 수집하지 않는가 | 원문·메모·주소·사진을 제외하고 내부 ID와 상태값만 수집 | 이벤트 속성 제한 및 4KB 상한 |
