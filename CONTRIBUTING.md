# 팀 작업 방식

`main`은 함께 실행되는 버전으로 유지합니다. 프론트 작업은 `frontend/`, 백엔드 작업은 `backend/`을 중심으로 별도 브랜치에서 진행하고 PR로 합칩니다. 두 영역 모두에 영향을 주는 API 변경은 `docs/개발_계약.md`와 `frontend/src/api.ts`를 같은 PR에서 수정합니다.

PR에는 바뀐 사용자 흐름, API 요청·응답 변화, 실행한 검증 명령을 적어 주세요. DB 파일·실제 가족 데이터·로그인 토큰은 커밋하지 않습니다. 프론트에서 백엔드가 아직 제공하지 않는 응답을 임의로 성공 처리하지 않고, 오류 또는 사용 가능한 수동 대체 흐름을 보여 줍니다.

백엔드 담당자는 `python -m unittest discover -s tests -v`, 프론트 담당자는 `npm run build`와 `npm run lint`를 통과시킵니다. Figma 화면 변경 시 해당 화면을 로컬 모바일 폭에서 확인합니다.
